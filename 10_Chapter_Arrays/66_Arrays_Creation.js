let browsers = ["Chrome", "Firefox", "Safari", "Edge"];

// array constructor
let scores = new Array(3); // creates an array with 3 empty slots
let scores2 = new Array(1, 2, 3); // creates an array with 3 elements: 1, 2, and 3



let chars = Array.from("hello"); // creates an array from a string, resulting in ['h', 'e', 'l', 'l', 'o']

let statuses ["Pass","Fail","Skip"]; // creates an array with 3 elements: "Pass", "Fail", and "Skip"

    

    