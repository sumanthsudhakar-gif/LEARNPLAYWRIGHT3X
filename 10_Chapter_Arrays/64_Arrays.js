let fruit = [];
let browsers = ["Chrome", "Firefox", "Safari", "Edge"];
console.log(browsers[0]); // Output: Chrome
console.log(browsers.at(-1)); // Output: Firefox
console.log(browsers.length); // Output: 4
console.log(fruit.length); // Output: 0


// for the nagative index we can use the at() method to access elements from the end of the array.
