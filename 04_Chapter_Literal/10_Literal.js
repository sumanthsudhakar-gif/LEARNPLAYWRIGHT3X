let count   = 10; // Integer literal assigned to the variable count
let negative = -9.99; // Floating-point literal assigned to the variable negative
let zero    = 0; // Integer literal assigned to the variable zero

let h = 0xFF; // Hexadecimal literal assigned to the variable h
console.log(h); // Output: 255
let color_hex = 0xFF00FF; // Hexadecimal literal assigned to the variable color_hex
let o = 0o77;
let 

let octal = 0o77; // Octal literal assigned to the variable octal
let b = 0b1010; // Binary literal assigned to the variable b
let million = 1_000_000; // Numeric literal with underscores for readability
let tiny = 1e-6; // Scientific notation literal assigned to the variable tiny

console.log(tiny); // Output: 0.000001

let float1 = 3.14; // Floating-point literal assigned to the variable floating
let float2 = _0.5; // Floating-point literal assigned to the variable float2
let float3 = .5; // Floating-point literal assigned to the variable float3
let float4 =  5.; // Floating-point literal assigned to the variable float4
console.log(float1); // Output: 3.14
console.log(float2); // Output: 0.5 
console.log(float3); // Output: 0.5
console.log(float4); // Output: 5.0
